import React from "react";
import { Container } from "./style";

export default function SearchField({children}) {
    return (
        <Container>
            {children}
        </Container>
    );
}
