import React from 'react'
import { Container } from './style'

export default function ScrollView({ children }) {
    return (
        <Container>
            {children}
        </Container>
    );
}
