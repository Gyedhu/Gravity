import React from "react";
import { Container, Icon, TextContent, BigText, Text } from "./style";

export default function Form({ iconClass, header, text }) {
    return (
        <Container>
        </Container>
    )
}
